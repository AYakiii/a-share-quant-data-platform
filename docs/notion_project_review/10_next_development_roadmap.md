# 10 Next Development Roadmap

## 短期（1-2 phase）
- 统一 feature store 构建路径（`features/store.py` vs `build_real_feature_store.py`）。
- 固化信号实验协议（样本切分、成本敏感性、稳定性阈值）。

## 中期（3-5 phase）
- risk-control module v1：暴露约束 + 风险预算 + 容量约束。
- benchmark 对照体系扩展（指数+等权+行业中性）。

## 长期
- base model 框架化（可插拔因子、可追踪参数、版本化报告）。
- reporting 自动化（tearsheet / Notion 同步 / artifact registry）。
